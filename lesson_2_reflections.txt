What happens when you initialize a repository? Why do you need to do it?

when we initialize a repository there are no commits to it and only the .git file is inserted into the repository


How can you use the staging area to make sure you have one commit per logical change?

in staging area all the changes to all the files are saved as a single commit